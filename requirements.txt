# =====================================================================
# Trade Pipeline — Runtime + dev dependencies
# Versiones mínimas alineadas con el stack del proyecto:
#   Python 3.10+ / Polars 1.0+ / Patito 0.7+ / Pydantic 2.x
# =====================================================================

# --- Data processing ---------------------------------------------------
polars>=1.0,<2.0
patito>=0.7
pyyaml>=6.0

# --- Generación de datos sintéticos -----------------------------------
faker>=20.0

# --- API REST ---------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0,<3.0

# --- HTTP client (extractor mode='api' + TestClient) ------------------
httpx>=0.27

# --- Testing ----------------------------------------------------------
pytest>=7.0
pytest-cov>=4.0
